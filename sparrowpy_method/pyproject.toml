[project]
name = "sparrowpy_interface"
version = "0.1.0"
description = "Sound Propagation with Acoustic Radiosity for Realistic Outdoor Worlds"
requires-python = ">=3.11,<3.15"
authors = [
    { name = "Anne Heimes", email = "ahe@akustik.rwth-aachen.de" },
]
keywords = [
    "acoustic simulation",
    "geometrical acoustics",
    "acoustic radiance transfer",
    "brdf",
    "scattering",
]

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Natural Language :: English",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "numpy>=1.23.0",
    "sparrowpy>=1.1",
    "requests",
    "gmsh",
    "trimesh",
    "pyfar",
    "pyrato",
]

[project.optional-dependencies]
deploy = [
    "twine",
    "wheel",
    "build",
    "setuptools",
    "bump-my-version",
]

tests = [
    "pytest",
    "pytest-cov",
    "watchdog",
    "ruff",
    "coverage",
]

docs = [
    "sphinx",
    "autodocsumm>=0.2.14",
    "pydata-sphinx-theme",
    "sphinx_mdinclude",
    "sphinx-design",
    "sphinx-favicon",
    "sphinx-reredirects",
]

dev = ["sparrowpy_interface[deploy,tests,docs]"]

# Source of truth for sparrowpy_interface's release version, scoped-tagged so
# it doesn't collide with the other methods sharing this repo.
[tool.bumpversion]
current_version = "0.1.0"
commit = true
tag = true
tag_name = "sparrowpy-v{new_version}"
message = "Bump sparrowpy_interface version: {current_version} → {new_version}"
tag_message = "Bump sparrowpy_interface version: {current_version} → {new_version}"

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = "version = \"{current_version}\""
replace = "version = \"{new_version}\""

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["sparrowpy_interface"]

[project.scripts]
sparrowpy_interface = "sparrowpy_interface:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
